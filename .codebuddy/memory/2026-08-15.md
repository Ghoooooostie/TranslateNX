# 2026-08-15

## 编译 + 部署 TranslateNX overlay
- 成功编译 `overlay/translatenx.ovl`（Switch overlay 模块，1,212,472 bytes）
- devkitpro 完整路径: **`D:\Program_Files\mysys2\opt\devkitpro`**（MSYS2 内安装）
- make 路径: **`D:\Program_FFiles\mysys2\usr\bin\make.exe`**
- 项目内 `e:\Project\TranslateNX\devkitpro/` 是不完整的副本（缺少编译器二进制和规则文件），不应使用
- 编译命令: `$env:DEVKITPRO="D:\Program_Files\mysys2\opt\devkitpro"; $env:PATH="D:\Program_Files\mysys2\usr\bin;"+$env:PATH; cd overlay; make clean; make`

## FTP 部署到 Switch (192.168.1.6)
- Switch IP: 192.168.1.6, FTP 端口 21, 匿名登录
- SD 卡路径: `/1. SD卡:/`（UTF-8 中文目录名需用 .NET FtpWebRequest 处理，MSYS ftp 不支持中文 cd）
- ovl 目标: `/1. SD卡:/switch/.overlays/translate.ovl`
- heap 配置目标: `/1. SD卡:/config/nx-ovlloader/heap_size.bin`

## 0x559 崩溃修复
- 原因: Switch 上无 `heap_size.bin`, HOS21+ 默认仅 4MB 堆 → TranslateNX(1.2MB) 加载时 OOM
- 修复: 写入 `heap_size.bin` = 16MB (0x1000000 u64 LE), overlay 正常运行
- OverlayHeapSize 枚举在 `libultrahand/libultra/include/tsl_utils.hpp:644`
- sys-ftpd rename 对中文路径返回 553（不支持）

## 新增 UI 显示语言：中文 (ZH)
- `L(tr, en)` 宏扩展为 `L(tr, en, zh)` 三参，按 `g_config.uiLang` 选择（"EN"→en, "ZH"→zh, 默认 tr）
- `main.cpp` 全部 38 处 `L()` 调用已补中文；`getLanguageName` 各语言名补中文；`SettingsGui::update` 用 `getLanguageName(g_config.uiLang)` 显示 UI 语言（原硬编码 EN/TR）
- UI 语言选择页 `UiLanguageSelectGui` 的 langs 列表由 `{"TR","EN"}` 改为 `{"TR","EN","ZH"}`
- `overlay/config.example.ini` 加注释：`ui_lang` 可选 tr/en/zh
- 已编译通过产出 `translatenx.ovl`（仅预存 warning: truncateUtf8 未使用，非本次引入）

## 提交 (25576f0)
- commit: `feat(ui): add Simplified Chinese as UI display language`
- 仅提交本次任务相关 4 文件：overlay/source/main.cpp、overlay/config.example.ini、deploy_ftp.ps1、.gitignore
- **未提交（预存改动，非本次任务）**：overlay/include/{config.hpp,ocr.hpp,translate.hpp}、overlay/source/{config.cpp,http_client.cpp,ocr.cpp,translate.cpp} —— 含 OpenAI Vision/OCR 支持、config 新字段、http_client 重构、超时调整等，属之前 session 遗留，用户尚未要求提交
- .gitignore 新增忽略 deploy_log.txt/build_log.txt/ftp_test.txt 临时日志
